Karman Kar Shows & Events — v7 Session Timing Upgrade
Date: 2026-05-07

Purpose
- Removes the admin-facing General Time field from Manage Shows.
- Keeps the legacy database 'time' column for old records/backward compatibility, but new create/update saves it blank.
- Adds session-level Cars Arrive Time to Registration Days / Sessions.
- Adds session-level Participant Instructions to Registration Days / Sessions.
- Displays arrival time and instructions on:
  - public registration page
  - placeholder claim page
  - public show page
  - admin show mode page
- Exports the added session fields in registration_slots.csv through the existing snapshot export.

Replacement files included
- app.py
- database.py
- templates/admin_shows.html
- templates/register.html
- templates/placeholder_claim.html
- templates/admin_show_mode.html
- templates/show.html
- templates/admin.html
- INSTALL_NOTES.txt

Recommended checks before commit
python -m py_compile app.py database.py sponsorship_blueprint.py
findstr /n "slot_1_cars_arrive_time" templates\admin_shows.html
findstr /n "participant_instructions" app.py database.py templates\register.html templates\placeholder_claim.html templates\show.html
findstr /n "General Time" templates\admin_shows.html

The General Time command should return no lines from templates\admin_shows.html.

Suggested git commands
git status --short
git add -A
git commit -m "Add session arrival times and participant instructions"
git push origin main
